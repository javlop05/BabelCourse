'use strict';

(function() {

    var prevWorks = $('#prev');
    var nextWorks = $('#next');
    var page = 0;

    /*
        function loadPhotos() {

        }


        setTimeout(function() {
            window.service.photo.getPhotos().then(function(responses) {
                console.log(responses);
                var html = responses.reduce(function(total, response, index, list) {
                    console.log(arguments);
                    return total + '<li><img src="' + response.url + '"alt="' + response.title + '"><h2>' + response.title + '</h2><p>lorem lorem</p></li>';
                }, '');
                $('#movies div').append(html);
            }).catch(function() {
                console.log(arguments);
            });
        }, 3000);
    */
})();
